# Generated by build_shakespeare*.py — committed shards would bloat the
# repo (~1-5 MB per build) and they're trivially regenerable. The build
# scripts are the source of truth.
data/
data-*/
cache/
